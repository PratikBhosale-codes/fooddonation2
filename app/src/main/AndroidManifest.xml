<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fooddonation">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Food Donation"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">

        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Sign Up Activity -->
        <activity android:name=".SignUpActivity" />

        <!-- Main Screen -->
        <activity android:name=".MainActivity"/>

        <!-- Donor Page -->
        <activity android:name=".DonateActivity" />
        <activity android:name=".AvailableFoodActivity" />

        <!-- NGO Agent Page -->
        <activity android:name=".NGOActivity" />
        <activity android:name=".PlacesActivity" />
        <activity android:name=".AshramListActivity" />
        <activity android:name=".AshramDetailActivity" />





    </application>

</manifest>
